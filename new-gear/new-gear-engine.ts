
import { newGearRecommendation } from './new-gear-recommendation';
import { newGearReferenceRange } from './new-gear-reference-range';

export const newGearEngine = {
  getReferenceRange: newGearReferenceRange,
  getRecommendation: newGearRecommendation,
};

